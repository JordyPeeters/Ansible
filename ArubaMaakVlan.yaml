- hosts: all
  collections:
    - arubanetworks.aoscx
  gather_facts: False
  connection: local  # Specify the connection type as HTTP API
  tasks:
    - name: Adding or Updating Banner
      aoscx_banner:
        banner_type: banner
        banner: "Hi!"
